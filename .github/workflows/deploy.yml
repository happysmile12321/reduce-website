name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v3
        with:
          node-version: 16
      - run: yarn install

      - name: Build
        run: yarn docs:build

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: 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
          publish_dir: public
          # cname: example.com # if wanna deploy to custom domain

